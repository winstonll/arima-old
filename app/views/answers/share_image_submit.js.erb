<%@question = Question.where(id: params[:question_id]).first
  @answer = Answer.new(question_id: @question.id, user_id: current_user.id)
puts @question.id
%>

$("#tag_show").html("<%= escape_javascript(render partial: 'questions/tag_input', locals: { question: @question, answer: @answer} %>");
