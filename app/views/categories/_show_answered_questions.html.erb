<% @answered.each do |question| %>
  <%= render partial: "common/question_normal", locals: { answered: true, answer: question.answers.where(user_id: cookies[:guest]).first, question: question } %>
<% end %>
