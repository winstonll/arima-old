<nav class="nav" role="navigation" id="home-navbar">
  <section class="left">
     <form class="form-horizontal" role="form" action="/search">
      <div class="form-group">
        <div class="col-sm-10">
          <input type="text" name="search_text" class="form-control" id="st" placeholder="Search">
        </div>
      </div>
    </form>
  </section>

  <section class="right">
    <%= link_to image_tag("logo-icon-large.png"), categories_path, id: "arima-icon" %>
    <% if user_signed_in? && current_user.username.present? %>
      <%= link_to "#{current_user.username[0].capitalize}", custom_show_path(:username => current_user.username), class: "nav-user-circle" %>
    <% end %>
    <%= link_to image_tag("anonprofile.png", size: "40x40"), new_registration_path(resource_name), id: "anonav-btn" if !user_signed_in? %>
    <%= link_to image_tag("imageupload.png", size: "22x20") + "  UPLOAD", new_question_path, id: "imageupload" %>
    <% if !(user_signed_in? && current_user.username.present?) %>
            <%= link_to 'Log In', new_user_session_path, class: "nav-btn hidden-xs", id: "nav-login-btn" %>
    <%else%>
      <%= link_to 'Log Out', destroy_user_session_path, :method => :delete, title: "#{current_user.email}", class: "nav-btn", id: "nav-logout-btn" %>
    <% end %>
  </section>
</nav>
