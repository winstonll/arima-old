require 'rails_helper'

RSpec.describe CategoriesController, :type => :controller do
  describe "Guest User" do
    pending "has no access to profile"
    pending "has question answer limit"
    pending "has access to other question categories"
    pending "can answer questions"
    pending "does not change user on redirect"
    pending "is saved in database"
    pending "categories index is populated with questions"
    pending "can return to homepage"
    pending "is prompted to signup after question limit is reached"
  end
end
